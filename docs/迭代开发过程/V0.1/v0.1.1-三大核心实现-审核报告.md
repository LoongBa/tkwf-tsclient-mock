# v0.1.1 — MockTransport / createMockFactory / createMockDb 审核报告

> **状态**：通过（代码审查 + 测试 + 构建全绿）
> **审核人**：Sisyphus
> **审核日期**：2026-08-12
> **版本**：v0.1.1
> **审核对象**：`src/mock-transport.ts` / `src/factory.ts` / `src/mock-db.ts` / `src/session.ts` / `src/index.ts` + 4 个测试文件

---

## 一、审核结论

| 项 | 结论 |
|----|------|
| 测试 | **47 passed**（4 文件：transport / factory / mock-db / session） |
| 构建 | `npm run build` 0 error |
| Oracle 强制项 | ✅ 全部落地（见 §三） |
| 代码纪律 | ✅ 无 `as any` / `@ts-ignore` / `@ts-expect-error`；无 `as T` 硬造 |
| **总体** | **通过**，可提交 v0.1.1 |

---

## 二、测试覆盖

| 文件 | 用例数 | 覆盖点 |
|------|--------|--------|
| `mock-transport.test.ts` | — | 分发 / delay / failRate / error / field 提取 / 多顶层 field / timeoutMs |
| `factory.test.ts` | — | 各类型生成 / 种子确定性 / 枚举 / 循环引用 / overrides / makeN / isId 自增 |
| `mock-db.test.ts` | — | FilterInput 过滤 / and/or 嵌套 / 深度限制 / SortInput 多字段 / 游标分页 / 偏移分页 / 删除行降级 / 状态同步 / reset / buildDataset |
| `session.test.ts` | 4 | requestChallenge / loginByContext（含 fallback）/ loginByPassword / logout |

> 实际 47 条用例，超过方案 §2.1–§2.4 的 25+ 规格。

---

## 三、Oracle 审核强制项逐条核验

| # | Oracle 要求 | 落地位置 | 核验 |
|---|-------------|----------|------|
| 1 | factory 运行时 schema（`MockFieldSchema` / `_types` / `_enums`） | `factory.ts:2-26` | ✅ |
| 2 | 循环引用防护 `_maxDepth`（默认 3） | `factory.ts:75` | ✅ |
| 3 | isId 严格匹配（仅 `field === "id"`） | `factory.ts:77` | ✅ |
| 4 | Date 输出 **Date 实例**（时间轴递增） | `factory.ts:91-94` | ✅ |
| 5 | 无 `as T` 硬造（运行时 schema 分支 → `Record<string, unknown>`，末尾一次受控断言） | `factory.ts:124,145` | ✅ |
| 6 | 游标分页 `base64({ id, sortValue })` | `mock-db.ts:170-179` | ✅ |
| 7 | 二分定位 + 删除行降级 | `mock-db.ts:200-206` | ✅ |
| 8 | and/or 隐式 AND 语义、深度 ≤5 | `mock-db.ts:86` | ✅ |
| 9 | `compareValues` 跨类型统一比较 | mock-db（叶子谓词） | ✅ |
| 10 | 包边界干净（仅 `import type` 主包） | 全部源文件 | ✅ |

---

## 四、代码纪律核验

- [x] 无 `as any` / `@ts-ignore` / `@ts-expect-error`
- [x] 无 `as T` 硬造（唯一断言在 `make` 末尾，Oracle 许可）
- [x] 无空 catch 块
- [x] TS7 `erasableSyntaxOnly` 兼容（无 enum/namespace）
- [x] 仅 `import type` 主包类型，运行时零主包依赖
- [x] Node 全局 `btoa/atob`（Node ≥16 无 polyfill 需求）

---

## 五、遗留事项

| 项 | 说明 | 归属 |
|----|------|------|
| 主包 transport 注入点 | `DomainHostClientOptions.transport` 5 行改动 | 主包 v1.1.0（另一仓库） |
| 消费端 codegen | `gen-mock-handlers.ts` 生成 handler 骨架 | v1.0.0 规划（P1） |
| setScenario / record-replay / 运行时契约校验 | 未来方向 | README 规划 |

---

## 六、变更文件清单

| 文件 | 变更 |
|------|------|
| `src/mock-transport.ts` | 两阶段 field 提取 + timeoutMs + 完善延迟/失败注入 |
| `src/mock-transport.test.ts` | 测试扩充 |
| `src/factory.ts` | 空壳 → schema 驱动完整实现 |
| `src/factory.test.ts` | 新增 |
| `src/mock-db.ts` | 查询语义（Filter/Sort/分页）+ 状态同步 + reset + 游标 |
| `src/mock-db.test.ts` | 新增 |
| `src/session.ts` | 骨架（v0.1.0 已有） |
| `src/session.test.ts` | 新增（4 用例） |
| `src/index.ts` | 补全新类型导出 |
| `README.md` | 新增（意义/对比/规划） |
| `docs/迭代开发过程/V0.1/v0.1.1-三大核心实现-开发方案.md` | 方案文档（Oracle 修订版） |
| `.gitignore` | 新增 `.opencode/` |
| `package-lock.json` | 新增（锁文件） |
