# v2.0.1 — 关联数据生成（`_relations`）审核报告

> **状态**：通过（全量测试/构建全绿）
> **版本**：v2.0.1

---

## 改动清单

| 文件 | 改动 | 说明 |
|------|------|------|
| `src/factory.ts` | 扩展 | `RelationFactoryConfig` 类型 + `MockFactoryOptions._relations` + `generateRelations` 函数（visited-set 循环防护 + FK 自动关联）+ `makeN` 调用关联生成 |
| `src/factory.test.ts` | 增强 | 2 新测试（`_relations` makeN 生成 + 向后兼容） |

---

## 测试

| 文件 | 结果 |
|------|------|
| 全量 | **230 passed**（15 文件，新增 2） |
| `npm run build` | 0 error |

---

## 代码纪律检查

- ✅ 无 `as any` / `@ts-ignore` / `@ts-expect-error`
- ✅ 向后兼容：不传 `_relations` 时行为与 v2.0.0 完全一致

---

## 交付物

- [x] 源代码：factory.ts / factory.test.ts
- [x] 测试：230 条全绿
- [x] 构建：npm run build 0 error