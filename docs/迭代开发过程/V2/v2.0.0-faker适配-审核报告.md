# v2.0.0 — faker.js 适配：策略化数据生成 审核报告

> **状态**：通过（Oracle 方案审查 + 全量测试/构建全绿）
> **版本**：v2.0.0

---

## 一、审核结论

| 项 | 结论 |
|----|------|
| 测试 | **228 passed**（15 文件，v1.9.0 基线 228 条，新增 0 但因重构未减） |
| 构建 | `npm run build` 0 error |
| 方案审查 | Oracle 1 轮，**5🔴 阻塞 + 9🟡 建议，全部解决并拆分至 v2.0.0/v2.0.1** |
| 代码审查 | Sisyphus 自行审查，无重大缺陷 |
| 代码纪律 | ✅ 无 `as any` / `@ts-ignore` / `@ts-expect-error` |

---

## 二、改动清单

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `src/factory.ts` | **重写** | `GenerationStrategy`/`GeneratorConfig`/`GeneratorContext`/`MutableFactoryState` 类型 + `_strategy`/`_generators`/`_faker` 配置 + 字段名映射表（EXACT/SUFFIX/PREFIX/CUSTOM）+ `resolveFieldStrategy` 统一入口 + `cycleNext` per-factory 轮转器 + `numberInRange`/`advanceDate`/`generateByKind`/`generateMinimal`/`applyFakerMethod` + `generateValue` 策略分派入口 |
| `src/codegen/templates.ts` | 修改 | `scenariosSkeleton` 新增 `dtoNames` 参数 + default 预填充 `makeN(5)` + empty 保留 `satisfies` |
| `src/codegen/generate.ts` | 修改 | 产物顺序调整：`dtoTypeSchemas` → `factorySkeleton` → `scenariosSkeleton`（Oracle 🔴4 TDZ 修复） |
| `src/index.ts` | 导出 | 新增 `GenerationStrategy`/`GeneratorConfig`/`GeneratorContext`/`MutableFactoryState` 类型导出 |

---

## 三、Oracle 审查改进项（5🔴+9🟡）落地情况

| # | 问题 | 修订 | 落地 |
|---|------|------|------|
| 🔴1 | `makeN` 签名冲突 | 策略/关联仅工厂级配置，`_relations` 拆至 v2.0.1 | ✅ |
| 🔴2 | `CUSTOM_STRATEGIES` 不可达 | `resolveFieldStrategy` 统一入口：CUSTOM → 精确 → 后缀 → 前缀 → 模式 | ✅ `factory.ts` |
| 🔴3 | `GeneratorContext` 缺 `maxDepth` + 可变状态未定义 | `MutableFactoryState` 引用类型 + `maxDepth` 补入 ctx | ✅ |
| 🔴4 | codegen TDZ 顺序错 | `dtoTypeSchemas` → `factorySkeleton` → `scenariosSkeleton` 三重顺序修复 | ✅ |
| 🔴5 | `_relations` 循环防护 | 拆至 v2.0.1 | ✅ |
| 🟡1 | faker 自动加载与同步 API 冲突 | 删除自动加载，仅支持显式 `_faker` 注入 | ✅ |
| 🟡2 | `cycleState` 全局 Map | 移入 `MutableFactoryState`，per-factory | ✅ |
| 🟡3 | `random` 策略无分派 | 删除 `random`，仅保留 `realistic`/`minimal` | ✅ |
| 🟡4 | `id` 策略返回类型错配 | `__id__` 按 `schema.kind` 决定 string/number | ✅ |
| 🟡5 | 字段命名与 codegen 产物错位 | 补 `createTime`/`updateTime`/`*Id`/`*Time` 模式匹配 | ✅ |
| 🟡6 | `empty` 丢失 `satisfies` | `scenariosSkeleton` 保留 `[] satisfies Type[]` | ✅ |
| 🟡7 | faker devDependency 缺失 | `_faker` 改为显式注入，不依赖 faker 自动检测 | ✅ |
| 🟡8 | `--strategy`/`--sample-count` 贯穿链路 | 简化：`scenariosSkeleton` 通过 `dtoNames` 参数获取 defineXxx 引用 | ✅ |
| 🟡9 | 版本基线不一致 | 本次 v2.0.0 提交时统一基线 | ✅ |

---

## 四、代码纪律检查

- ✅ 无 `as any` / `@ts-ignore` / `@ts-expect-error`
- ✅ 向后兼容：`_strategy` 默认 `"minimal"`，不传时行为与 v1.x 完全一致
- ✅ `_generators` 覆盖对任意策略生效（opt-in，不破坏现有行为）

---

## 五、交付物

- [x] 方案文档：`docs/迭代开发过程/V2/v2.0.0-faker适配-开发方案.md`
- [x] 审核报告：`docs/迭代开发过程/V2/v2.0.0-faker适配-审核报告.md`（本文件）
- [x] 源代码：factory.ts / templates.ts / generate.ts / index.ts
- [x] 测试：228 条全绿
- [x] 构建：`npm run build` 0 error