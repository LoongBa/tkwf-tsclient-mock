# v1.3.0 — 录制回放（record-replay）审核报告

> **状态**：通过（方案 Oracle 审查 + 实现 + 全量测试/构建全绿）
> **审核人**：Sisyphus（方案 + 实现 + 代码审查）+ Oracle（方案审查）
> **审核日期**：2026-08-12
> **版本**：v1.3.0
> **审核对象**：`src/record-replay.ts`（新建核心）+ `src/file-recording-store.ts`（新建文件存储）+ `src/index.ts`（导出）+ `README.md`（录制回放章节）

---

## 一、审核结论

| 项 | 结论 |
|----|------|
| 测试 | **161 passed**（13 文件，较 v1.2.0 131 条新增 30 条，含 record-replay 21 + file-recording-store 9） |
| 构建 | `npm run build` 0 error |
| 方案审查 | Oracle 1 轮，**2🔴 阻塞 + 6🟡 建议，全部解决并入方案 v2** |
| 代码审查 | Sisyphus 自行审查（非复杂实现），无重大缺陷 |
| 方案遵循度 | ⭐⭐⭐⭐⭐（§3.1-§3.7 逐项对应，无偏离） |
| 代码纪律 | ✅ 无 `as any` / `@ts-ignore` / `@ts-expect-error` |

---

## 二、改动清单

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `src/record-replay.ts` | **新建** | `RecordingStore` 接口 + `MemoryRecordingStore` + `configureRecordingStore` + `createRecordingTransport` 装饰器 + `MockRecordingError` + `normalizeTimestamps`/`normalizeUuids` |
| `src/record-replay.test.ts` | **新建** | 21 测试（MemoryRecordingStore 5 / record 模式 3 / replay 模式 9 / passthrough 1 / configureStore 1 / 归一化器 3） |
| `src/file-recording-store.ts` | **新建** | `FileRecordingStore` 实现 `RecordingStore` 接口，同步 `node:fs` 持久化 |
| `src/file-recording-store.test.ts` | **新建** | 9 测试（写入/读取/列出/目录自动创建/生命周期约束） |
| `src/codegen/node-types.d.ts` | 修改 | 新增 `fs.mkdirSync`/`fs.readdirSync` 声明 |
| `src/index.ts` | 扩展 | 导出 record-replay + file-recording-store 全部新 API |
| `README.md` | 更新 | 核心能力表 + 录制回放章节 + 路线图 |

---

## 三、方案 vs 实现对照

| 方案 § | 要求 | 实现 | 状态 |
|--------|------|------|------|
| §3.1 录制模型 | `RecordedEntry`/`Recording` 类型 + 匹配键 `field+type+归一化variables` | `makeMatchKey` 键排序 + `undefined`→`{}` | ✅ |
| §3.2 RecordingStore | 接口允许 Promise（异步兼容）+ 内存默认 + `configureRecordingStore` | `void\|Promise<void>` 混合返回类型；`MemoryRecordingStore`；生命周期约束 | ✅ |
| §3.3 createRecordingTransport | 装饰器 + 三态 + `executeRawGraphQL` 双入口 + `MockRecordingError` + `order`/`maxUsageCount`/`onMiss`/`strictVersion` | 完整实现，`WeakMap` 消费计数 | ✅ |
| §3.4 归一化器 | `normalizeTimestamps`/`normalizeUuids` 递归替换 | ISO-8601 regex + UUID v1-v5 regex | ✅ |
| §3.5 FileRecordingStore | 同步 `node:fs`，`<dir>/<name>.json` 格式 | 完整实现 + 生命周期约束 | ✅ |
| §3.6 场景集成 | 正交，录制名含场景仅消费端约定 | documenting via README | ✅ |
| §3.7 导出 | `index.ts` 导出全部新 API | 完整导出 | ✅ |
| §四 测试 | 15+ 用例 | **30 新增**（record-replay 21 + file-recording-store 9） | ✅ |

---

## 四、测试覆盖明细

| 测试文件 | 新增用例 | 覆盖 |
|---------|---------|------|
| `record-replay.test.ts` | 21 | MemoryRecordingStore 生命周期 5 / record 模式 3（含 normalizeResult）/ replay 模式 9（命中/未命中/error条目/maxUsage/variables键排序/rawGraphQL/version检查/signal中止）/ passthrough 1 / configureStore 1 / 归一化器 3 |
| `file-recording-store.test.ts` | 9 | 写入录制 / 读取录制 / 列出录制 / 目录自动创建 / 不存在的录制返回 undefined / record 在 start 前抛错 / stop 在 start 前抛错 / 重复 start 抛错 / stop 后重新 start |

---

## 五、方案审查改进项（Oracle 2🔴+6🟡）落地情况

| # | 问题 | 方案 v2 修订 | 落地 |
|---|------|-------------|------|
| 🔴1 | RecordingStore 全同步 → 不兼容 IndexedDB | 接口允许 `Promise` 返回类型 | ✅ `record-replay.ts:38-47` |
| 🔴2 | `executeRawGraphQL` 伪代码缺失 | §3.3 补全三态分支 | ✅ `record-replay.ts:355-400` |
| 🟡1 | `error.source` 缺真实后端类型 | 扩展 `"transport"`/`"network"` + `errorCode` | ✅ `RecordedEntry.error` |
| 🟡2 | 归一化器误伤 variables | 默认仅用于 `normalizeResult`，文档标注 | ✅ README 提示 |
| 🟡3 | `order=false` 多匹配选择策略 | 取第一个可消费条目（确定性） | ✅ `record-replay.ts:283-288` |
| 🟡4 | 回放错误缺类型信息 | `MockRecordingError` 携带 source/errorCode | ✅ `record-replay.ts:64-75` |
| 🟡5 | 生命周期约束不明确 | JSDoc 明确契约 + 实现强制检查 | ✅ 所有 3 个 store 实现 |
| 🟡6 | version 不匹配检查缺失 | `strictVersion` 选项 + 默认 console.warn | ✅ `record-replay.ts:271-278` |

---

## 六、代码纪律检查

- ✅ 无 `as any`
- ✅ 无 `@ts-ignore` / `@ts-expect-error`
- ✅ 无未使用的导出
- ✅ 无死代码
- ✅ 向后兼容：纯装饰器模式，`MockTransport`/`Transport` 零改动
- ✅ 类型安全：`RecordingStore` 返回类型为 `void|Promise<void>` 联合

---

## 七、交付物

- [x] 方案文档：`docs/迭代开发过程/V1/v1.3.0-record-replay-开发方案.md`（v2 定稿）
- [x] 审核报告：`docs/迭代开发过程/V1/v1.3.0-record-replay-审核报告.md`（本文件）
- [x] 源代码：record-replay.ts / file-recording-store.ts / index.ts / README
- [x] 测试：161 条全绿
- [x] 构建：`npm run build` 0 error

---

## 八、待定事项

- **tag 发布**：需用户确认后执行 `git tag v1.3.0` + `git push origin v1.3.0`（触发 npm publish 自动发布）