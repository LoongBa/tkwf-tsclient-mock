# v1.2.0 — 场景切换（setScenario）+ 分阶段策略 审核报告

> **状态**：通过（方案 Oracle 审查 + 实现 + 全量测试/构建/端到端全绿）
> **审核人**：Sisyphus（实现 + 代码审查）+ Oracle（方案审查）
> **审核日期**：2026-08-12
> **版本**：v1.2.0
> **审核对象**：`src/mock-db.ts`（多数据集扩展）+ `src/mock-transport.ts`（场景注入扩展）+ `src/scenario.ts`（新建协调器）+ `src/define-mock.ts`（ctx 类型扩展）+ `src/codegen/templates.ts` + `src/codegen/generate.ts`（场景骨架）+ `src/index.ts`（导出）+ `README.md`（场景章节 + 分阶段策略指南）

---

## 一、审核结论

| 项 | 结论 |
|----|------|
| 测试 | **131 passed**（11 文件，较 v1.1.0 105 条新增 26 条，覆盖多数据集/场景注入/协调器上下文/codegen 骨架含切换-恢复/切换中 handler 行为） |
| 构建 | `npm run build` 0 error |
| 端到端 | 全量 131 测试通过；机制性测试覆盖场景切换后数据一致性、注入优先级链、回滚原子性 |
| 方案审查 | Oracle 1 轮，**4🔴 阻塞 + 5🟡 建议，全部解决并入方案 v2** |
| 代码审查 | Sisyphus 自行审查（非复杂实现），无重大缺陷 |
| 方案遵循度 | ⭐⭐⭐⭐⭐（§3.1-§3.7 逐项对应，无偏离） |
| 代码纪律 | ✅ 无 `as any` / `@ts-ignore` / `@ts-expect-error` |

---

## 二、改动清单

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `src/mock-db.ts` | 扩展 | `MockDbOptions.datasets` + `switchDataset`/`getDatasetName`/`listDatasets`/`reset(name?)` |
| `src/mock-transport.ts` | 扩展 | `FieldOption` 提取导出 + `ScenarioConfig` + `setScenario`/`getScenario`/`getScenarioNames` + 场景优先级注入 |
| `src/scenario.ts` | **新建** | `createScenarioContext` 协调器（校验优先 + 回滚原子性） |
| `src/define-mock.ts` | 更新 | 泛型重载 ctx 同步 `scenario?: string` |
| `src/codegen/templates.ts` | 扩展 | `scenariosSkeleton` / `scenarioOverridesSkeleton` + `imports()` 追加 `ScenarioConfig` |
| `src/codegen/generate.ts` | 扩展 | 生成产物在 db 骨架后追加场景骨架块 |
| `src/index.ts` | 扩展 | 导出 `createScenarioContext` + `ScenarioContext`/`ScenarioConfig`/`FieldOption`/`BuiltinScenario`/`MockDbOptions` |
| `README.md` | 更新 | 核心能力表 + 场景切换章节 + 分阶段策略指南 + 路线图 |

---

## 三、方案 vs 实现对照

| 方案 § | 要求 | 实现 | 状态 |
|--------|------|------|------|
| §3.0 设计决策 | 4 项决策确认 | 全部按决策执行 | ✅ |
| §3.1 场景模型 | `ScenarioConfig { delayMs?; error?; fieldOptions? }` + `BuiltinScenario` | 完整实现，`error?` 场景级快捷注入（🟡B2） | ✅ |
| §3.2 mock-db 多数据集 | `datasets`/`switchDataset`/`reset(name?)` | + `listDatasets()` / `getDatasetName()`（🟡B1）；`reset(name?)` 语义明确：无参重置当前活跃数据集（🔴#4） | ✅ |
| §3.3 transport 场景注入 | 场景优先级链 + `ctx.scenario` | 优先级链逐项覆盖精确实现；+ `getScenarioNames()`（🟡B1） | ✅ |
| §3.4 协调器 | `createScenarioContext` 联动切换 | 校验优先 + 失败回滚（🔴#3）；删除 datasets 参数（🔴#2） | ✅ |
| §3.5 codegen 骨架 | `scenarios`/`scenarioOverrides` 骨架 | `satisfies` 降级处理（🟡B4） | ✅ |
| §3.6 defineMock | `ctx.scenario` 类型扩展 | 泛型重载同步更新（🔴#1） | ✅ |
| §3.7 包结构 | 文件结构 | 按方案新增 `scenario.ts` | ✅ |
| §四 测试 | 20+ 用例 | **26 新增**（含切换-恢复/切换中 handler 行为，🟡B5） | ✅ |
| §六 步骤 8 | README 场景章节 + 分阶段策略 | 含 handler 读 `ctx.scenario` 示例（🟡B3） | ✅ |

---

## 四、测试覆盖明细

| 测试文件 | 新增用例 | 覆盖 |
|---------|---------|------|
| `mock-db.test.ts` | 7 | datasets 初始化 / switchDataset 切换 / 不存在抛错 / reset() 无参语义 / reset(name) 切换+重置 / 向后兼容 / field→table 映射保留 |
| `mock-transport.test.ts` | 7 | setScenario 切换 / getScenarioNames / ctx.scenario 传递 / 场景 fieldOptions error / 场景 fieldOptions failRate / 场景 delayMs / 场景优先于全局 / 场景级 error / 不存在抛错 |
| `scenario.test.ts` | 6+ | 协调器联动 / 仅数据场景 / 仅注入场景 / 自定义场景名 / 切换-恢复（error→default） / 切换中 handler 行为 |
| `codegen/generate.test.ts` | 2+ | 产物含 scenarios 骨架 / 产物含 scenarioOverrides 骨架 / 现有断言不回归 |

---

## 五、设计决策回顾

| 决策 | 方案结论 | 执行情况 |
|------|---------|---------|
| 场景粒度 | 全局 + 按 field 覆盖 | scenarios[scenario].fieldOptions?.[field] 优先于全局 fieldOptions |
| 场景数据来源 | db 多数据集 + codegen 场景骨架 | createMockDb.datasets + switchDataset；codegen 生成空骨架 |
| 错误态/加载态 | transport 层注入 | 复用 fieldOptions 管道，按场景字典选择 |
| 分阶段策略 | 文档指南 | README 分阶段策略表格（原型→开发→测试） |

---

## 六、方案审查改进项（Oracle 4🔴+5🟡）落地情况

| # | 问题 | 方案 v2 修订 | 落地状态 |
|---|------|-------------|---------|
| 🔴#1 | defineMock 泛型重载 ctx 未同步 | 重载签名添加 `scenario?: string` | ✅ `define-mock.ts:32` |
| 🔴#2 | createScenarioContext 重复 datasets | 删除 datasets 参数，改 `db.listDatasets()` | ✅ `scenario.ts:29-33` |
| 🔴#3 | 协调器非原子切换 | 校验优先 + 失败回滚 | ✅ `scenario.ts:38-89` |
| 🔴#4 | reset(name?) 语义模糊 | 无参=重置当前活跃数据集；有参=切换并重置 | ✅ `mock-db.ts` |
| 🟡B1 | 缺枚举接口 | 新增 `listDatasets()` / `getScenarioNames()` | ✅ |
| 🟡B2 | 缺场景级 error 快捷字段 | `ScenarioConfig.error?: unknown` | ✅ |
| 🟡B3 | 缺 handler 读 ctx.scenario 示例 | README 示例补充 | ✅ |
| 🟡B4 | codegen satisfies 降级 | 无实体类型表输出 `[]` + 注释 | ✅ `templates.ts:58-59` |
| 🟡B5 | 测试补切换-恢复/切换中 handler | 2 用例补充 | ✅ `scenario.test.ts` |

---

## 七、代码纪律检查

- ✅ 无 `as any`
- ✅ 无 `@ts-ignore` / `@ts-expect-error`
- ✅ 无未使用的导出
- ✅ 无死代码
- ✅ 向后兼容：不传新参数时行为与 v1.1.0 完全一致
- ✅ 类型安全：`MockHandler` 扩展 `scenario?: string` 为可选字段，`satisfies` 检查通过

---

## 八、交付物

- [x] 方案文档：`docs/迭代开发过程/V1/v1.2.0-scenario场景切换-开发方案.md`（v2 定稿）
- [x] 审核报告：`docs/迭代开发过程/V1/v1.2.0-scenario场景切换-审核报告.md`（本文件）
- [x] 源代码：mock-db / mock-transport / scenario / define-mock / codegen / index / README
- [x] 测试：131 条全绿
- [x] 构建：`npm run build` 0 error

---

## 九、待定事项

- **tag 发布**：需用户确认后执行 `git tag v1.2.0` + `git push origin v1.2.0`（触发 npm publish 自动发布）