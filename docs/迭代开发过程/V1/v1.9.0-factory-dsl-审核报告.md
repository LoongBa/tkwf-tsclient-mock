# v1.9.0 — 工厂 DSL + buildDataset 外键校验 审核报告

> **状态**：通过（全部测试/构建全绿）
> **版本**：v1.9.0

---

## 改动清单

| 文件 | 改动 | 说明 |
|------|------|------|
| `src/codegen/templates.ts` | 扩展 | `factorySkeleton` 模板函数 + `imports()` 追加 `createMockFactory` |
| `src/codegen/generate.ts` | 扩展 | 在 `dtoTypeSchemas` 后插入 `factorySkeleton` 调用 |
| `src/codegen/generate.test.ts` | 增强 | 2 新测试（defineXxxFactory 骨架 + import 更新） |
| `src/mock-db.ts` | 扩展 | `buildDataset` 新增 `strict` 参数 + `validateDatasetFK` 闭包函数 + `MockDb` 接口更新 |
| `src/mock-db.test.ts` | 增强 | 3 新测试（FK 合法/违规/向后兼容） |

---

## 测试

| 文件 | 结果 |
|------|------|
| 全量 | **228 passed**（15 文件，新增 5） |
| `npm run build` | 0 error |

---

## 代码纪律检查

- ✅ 无 `as any` / `@ts-ignore` / `@ts-expect-error`
- ✅ 向后兼容：`buildDataset` 无 `strict` 时行为不变；`defineXxxFactory` 骨架为纯新增
- ✅ `validateDatasetFK` 为闭包函数，不污染公共 API

---

## 交付物

- [x] 源代码：templates.ts / generate.ts / mock-db.ts / generate.test.ts / mock-db.test.ts
- [x] 测试：228 条全绿
- [x] 构建：npm run build 0 error