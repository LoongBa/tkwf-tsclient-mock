# v1.7.1 — codegen 关系推导（收尾补完）

> **状态**：通过（全部测试/构建全绿）
> **版本**：v1.7.1
> **说明**：v1.7.0 方案中设计但未实现的 codegen 关系推导收尾

---

## 改动清单

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `src/codegen/parse-dto.ts` | 扩展 | `inferDtoRelations` 函数 + `DtoRelationInference` 类型（FK 字段门控、自引用排除、belongsTo/hasMany 双向推断） |
| `src/codegen/templates.ts` | 扩展 | `relationSkeleton` 模板 + `imports()` 追加 `MockDb` 类型导入 |
| `src/codegen/generate.ts` | 扩展 | 在 scenario 骨架后插入 `inferDtoRelations` + `relationSkeleton` |
| `src/codegen/__fixtures__/sample.g.ts` | 扩展 | PaymentLog 新增 `merchantId: number` + `merchant?: Merchant`；Merchant 新增 `logIds: number[]` + `logs?: PaymentLog[]` |
| `src/codegen/generate.test.ts` | 增强 | 2 新测试（registerRelation 骨架包含 + 自引用跳过） |

---

## 测试

| 文件 | 结果 |
|------|------|
| 全量 | **211 passed**（14 文件，新增 2） |
| `npm run build` | 0 error |

---

## 代码纪律检查

- ✅ 无 `as any` / `@ts-ignore` / `@ts-expect-error`
- ✅ 向后兼容：`inferDtoRelations` 是纯新增，不改现有 codegen 产物结构
- ✅ FK 字段门控：无 FK 字段的 DTO 引用不推断（避免误判嵌入为关联）

---

## 交付物

- [x] 源代码：parse-dto.ts / templates.ts / generate.ts / sample.g.ts / generate.test.ts
- [x] 测试：211 条全绿
- [x] 构建：`npm run build` 0 error