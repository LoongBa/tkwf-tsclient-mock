# v1.4.3 — gen-seed CLI + session handlers 补齐 + importDatasetSeed 透传 — 审核报告

> **审核人**：Sisyphus（自审；方案经 Momus 审核 PASS + Sisyphus 自审 3 条观察全部纳入实现）
> **审核日期**：2026-08-19
> **版本**：v1.4.3
> **关联**：ADR09-Mock数据基础设施架构、v1.4.3-gen-seed-开发方案.md

---

## 审核范围

| 维度 | 范围 |
|------|------|
| 代码 | `src/codegen/gen-seed-core.ts`（新建）、`src/codegen/gen-seed.ts`（新建）、`src/dataset-io.ts`（修改）、`src/session.ts`（修改）、`package.json`（bin） |
| 测试 | `src/dataset-io.test.ts`（新增 4 用例）、`src/session.test.ts`（新增 6 用例 + 修改 2 既有用例） |
| 文档 | `README.md`、`skills/tkwf-tsclient-mock/SKILL.md` |
| 同步 | `_TKWF/docs/AC-Kit/skills/tkwf-tsclient-mock/`（2 文件）、`_TKWF/docs/D07M-*.md`、`_TKWF/docs/G07D-*.md` |
| 构建 | `npm run build` + `npx vitest run` + `npx tsc --noEmit` |

---

## 审核结果

**结论：通过** ✅

| 检查项 | 结果 |
|--------|------|
| 编译通过（`npx tsc --noEmit`） | ✅ 零新增错误 |
| 构建通过（`npm run build`） | ✅ |
| 测试通过（`npx vitest run`） | ✅ 290/290 通过（9 个新增） |
| 禁止模式（`as any` / `@ts-ignore` / `@ts-expect-error`） | ✅ 零违规（faker 类型用 `as unknown as Record` 双重转换） |
| 导出架构（主入口 / 子路径分离） | ✅ gen-seed-core 在 codegen 子目录，CLI 编译到 dist/codegen/ |
| 未使用导出/死代码 | ✅ 零 |

---

## 符合性检查

### 与开发方案对照

| 方案项 | 状态 | 说明 |
|--------|------|------|
| gen-seed CLI（bin） | ✅ | `npx gen-seed --spec mock-data-spec.json` 输出 seed.json |
| gen-seed-core.ts 核心逻辑提取 | ✅ | `generateSeedFile()` 可测函数，CLI 薄壳调用 |
| `--faker` 动态 import | ✅ | `await import("@faker-js/faker")`，失败降级 + warn |
| `--scenario` 覆盖 count | ✅ | 测试验证 empty 场景 → 0 条 |
| importDatasetSeed unknownTables 透传 | ✅ | `importDatasetSeed(db, path, { unknownTables })` |
| defaultSessionHandlers 补 4 handler | ✅ | loginBySms / ping / registerSecure / loginByQrCode |
| 修复 loginByPassword / loginByContext 补 success | ✅ | 两个 handler 返回 `success: true` + `displayName` |
| SKILL.md Step 4b gen-seed 指引 | ✅ | 含代码示例 |
| G07D 版本表 + 版本速查 | ✅ | gen-seed / importDatasetSeed / session handlers 行 |
| D07M 版本表 | ✅ | v1.4.3 行（290 测试） |
| AC-Kit 同步 | ✅ | 双仓库提交 |

### 自审三条观察全部纳入

| # | 观察 | 处理 | 结果 |
|---|------|------|------|
| ① | gen-seed faker async | `main()` 为 async，`await import("@faker-js/faker")` | ✅ 实现 |
| ② | 现有 handler 缺 success | loginByPassword / loginByContext 补 `success: true` + `displayName` | ✅ 修复 + 测试 |
| ③ | gen-seed 测试策略 | 提取 `gen-seed-core.ts` 放 `generateSeedFile()`，测试直接调用核心函数 | ✅ 实现 |

---

## 代码质量评估

### gen-seed-core.ts（48 行）

**优点**：
- 核心逻辑提取为 `generateSeedFile()` 可测函数，CLI 薄壳仅做参数解析——与 gen-mock-handlers 模式一致
- `faker` 参数类型 `Record<string, unknown>` 与 `generateFromSpec` 签名一致
- `path.dirname` + `mkdirSync({ recursive: true })` 正确处理嵌套目录创建
- JSDoc 完整，含 `@example`

**问题**：无。

### gen-seed.ts（73 行）

**优点**：
- 手写参数解析与 `cli.ts` 模式一致，零外部依赖
- `--faker` 动态 import 用 try/catch 降级 + console.warn，不影响主流程
- `main()` 为 async，正确处理 faker 动态导入

**问题**：无。

### session.ts（60 行，+36 行）

**优点**：
- 4 个新 handler 与现有 handler 结构一致
- `loginBySms` 从 `variables.mobile` 取值（与 `loginByPassword` 从 `variables.userName` 取值的模式一致）
- 修复 `loginByPassword` / `loginByContext` 补 `success: true` + `displayName`——消费端 `if (!payload.success)` 不再误判失败
- 注释标注 v1.4.3 修复/新增

**问题**：无。

### dataset-io.ts（+5 行）

**优点**：
- `options?: { unknownTables? }` 可选参数，不传则 `undefined` → buildDataset 默认 ignore → 完全向后兼容
- JSDoc 标注 v1.4.3

**问题**：无。

### 测试（+9 用例，290 总计）

**覆盖**：
- gen-seed：生成合法 seed.json + scenario 覆盖 count（2 用例）
- importDatasetSeed：warn 透传 + 无 options 默认 ignore（2 用例）
- session handlers：loginBySms + ping + registerSecure + loginByQrCode + 修复 loginByPassword + 修复 loginByContext（6 用例，含 fallback 测试）

**缺口**：gen-seed CLI 参数解析（`--spec` 缺失时退出码 1）未单独测试——核心逻辑已由 `generateSeedFile` 测试覆盖，CLI 薄壳的参数解析属实现细节，非阻塞。

---

## 建议

1. **（可选）gen-seed CLI 参数解析测试**：可用 `child_process.execSync` spawn gen-seed 进程验证 `--spec` 缺失时退出码 1
2. **（后续）loginByQrCode 主包实现后调整 mock 返回值**：当前防御性添加，主包 `domain-client-user.ts` 无对应方法实现，未来实现后需据实际 selection 调整 mock 返回字段

---

## 审核结论

**通过**。gen-seed CLI 填补了"一键导出 seed.json"的工具空白，session handlers 补齐消除了消费端 "no handler" 错误，importDatasetSeed 透传完善了接收方导入路径。自审三条观察全部在本次迭代中修复，未遗留技术债。

| 决策 | 内容 |
|------|------|
| 发布 | 可发布，需确认 tag（v1.4.3）和 npm publish |
| 文档同步 | 已同步（AC-Kit skill 副本 + D07M + G07D） |
| 后续版本 | gen-seed CLI 参数解析测试、loginByQrCode 主包实现后调整 |