# Mock 数据规格 (MOCK_SPEC)

> 本文件定义每张 mock 数据表填什么数据、填多少、字段取什么值。
> **Agent 或开发者填充 `<项目>/src/mock/data.ts` 时，以本文件为数据策略依据。**
>
> 维护方式：
> - **一、API 映射表**：由 `gen-mock-handlers --mock-spec` 自动生成（幂等更新标记段）
> - **二、数据策略**：手写，随需求变更更新
> - **三、表间关系**：手写，随数据模型变更更新

---

## 一、API → 数据表映射

> 每张表对应哪些 API。Agent 填充某张表时，知道会影响哪些页面。
> 自动生成，与 `ts-client.mock.g.ts` 的 `// → API:` 注释对齐。

<!-- auto-generated: mapping-table -->
（由 `gen-mock-handlers --mock-spec` 填充）
<!-- end-auto-generated -->

---

## 二、数据策略

> ⚠️ **填写指引**：
> 1. 每行对应 `src/mock/data.ts` 的 `initialData` 中一条数组记录
> 2. **最少条数**：确保该 API 页面不空即可，不是越多越好
> 3. **关键字段约束**：字段值必须满足什么条件（如 `success=true`、`totalAmount > 0`）
> 4. 参考文档：
>    - 字段名和类型 → `schema.graphql` 中对应 DTO 类型定义
>    - 字段含义和业务规则 → `DOMAIN_MAP.md` 实体字段描述 + `Business.md` 业务规则
>    - 业务流程 → 项目需求文档（如 `docs/Rxx-*.md`）
> 5. 边界态（空数据/错误态）由 `scenarioOverrides` 覆盖，不在 `initialData` 中模拟
> 6. 金额字段在个位数到万位数之间，不出现 0 或负数
> 7. 时间字段使用 `new Date()` 或相对当前时间的 ISO 字符串

### {业务域 1}

| 表 | 最少条数 | 关键字段约束 | 填写参考 |
|----|---------|-------------|---------|
| `{tableName}` | （填） | （填） | `文档引用` |

### {业务域 2}

| 表 | 最少条数 | 关键字段约束 | 填写参考 |
|----|---------|-------------|---------|
| `{tableName}` | （填） | （填） | `文档引用` |

---

## 三、表间关系

> 外键约束说明。填充 `data.ts` 时需保证引用一致性。
> 参考 `DOMAIN_MAP.md` 各实体字段定义确认外键字段名。

| 表 | 外键字段 | 关联表 | 说明 |
|----|---------|--------|------|
| `{tableName}` | `{fkField}` | `{targetTable}` | （说明） |

---

## 四、schema 变更后的同步流程

当 `schema.graphql` 变更（新增 field、新增 API、改字段类型）时，按顺序执行：

```bash
buildSchema.ps1 -Quick                                        # ① 更新 schema.graphql
npm run gen-ts-client                                          # ② 更新 ts-client.g.ts（类型 + 服务接口）
npm run gen-mock                                               # ③ 更新 ts-client.mock.g.ts（新 handler 骨架 + // → API: 注释）
gen-mock-handlers --mock-spec <本文件路径>                     # ④ 更新本文件第一节映射表
```

执行第③④步后：
1. `ts-client.mock.g.ts` 的 `createMockDb` 会自动新增/删除表，并更新 `// → API:` 注释
2. `--mock-spec` 幂等更新本文件第一节的 API 映射表
3. 如果新增了表，补充第二节数据策略、第三节表间关系
4. 编辑 `src/mock/data.ts` 补充新表的初始数据

---

## 五、MockDataSpec JSON（结构化规则）

> 本文件是人类可读的数据策略文档。**MockDataSpec JSON** 是其机器可读的结构化版本，两侧通过 `generateFromSpec()` 生成数据。
>
> 参考文件：
> - JSON Schema：`skills/tkwf-tsclient-mock/mock-data-spec.schema.json`
> - 完整示例：`skills/tkwf-tsclient-mock/MOCK_DATA_SPEC.example.json`

填写完本文件的数据策略后，将策略翻译为 MockDataSpec JSON：
- 第二节的"最少条数" → `entities.<entity>.count`
- 第二节的"关键字段约束" → `entities.<entity>.fields.<field>.strategy / distribution / min / max`
- 第三节的表间关系 → `entities.<entity>.relations`
- 默认场景 count → `scenarios.default` —— v1.4.1 起 `generateFromSpec(spec, { scenario })` 按场景覆盖 count；运行时场景切换（数据+注入联动）用 `createScenarioContext`

详情见 `MOCK_DATA_SPEC.example.json`。

---

## 六、共享与移植指引（v1.4.2）

> 本文件在不同项目间共享时，注意各节的移植性差异。

| 节 | 可移植性 | 接收方操作 |
|----|---------|-----------|
| §1 API 映射表 | ❌ 项目特定 | 删除 `<!-- auto-generated -->` 标记段，用本项目 `ts-client.g.ts` 重跑 `gen-mock-handlers --mock-spec` 重新生成 |
| §2 数据策略 | ✅ 可移植 | 同 WebApi 可直接复用；不同 WebApi 需按本项目实体调整 |
| §3 表间关系 | ✅ 可移植 | 同 WebApi 可直接复用；不同 WebApi 需按本项目外键调整 |
| §4 schema 变更同步 | ✅ 可移植 | 命令通用，路径按本项目调整 |
| §5 MockDataSpec JSON | ✅ 可移植 | 语言无关的结构化规则，直接复用 |

**接收方导入步骤**：
1. 复制本文件到接收项目
2. 删除 §1 映射表 → 重跑 `gen-mock-handlers --mock-spec` 重新生成
3. 若同时接收 `MockDataSpec JSON` → 直接 `generateFromSpec` 生成数据
4. 若同时接收 `DatasetSeed JSON` → 直接 `parseDatasetSeed` + `buildDataset` 加载
5. `buildDataset(seed, { unknownTables: "warn" })` 检测表名不匹配（v1.4.2）